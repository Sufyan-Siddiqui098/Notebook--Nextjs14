import React from 'react'
import UserProfile from "./page"

export const metadata = {
    title: "Notebook | Profile"
  };

const layout = () => {
  return (
    <UserProfile />
  )
}

export default layout