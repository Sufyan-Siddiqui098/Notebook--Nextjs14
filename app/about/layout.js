import React from 'react'
import AboutPage from './page';

export const metadata = {
    title: "Notebook | About",
    description: "The page containting the information about developer and the website.",
  };

const layout = () => {
  return (
    <AboutPage />
  )
}

export default layout