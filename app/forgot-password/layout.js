import React from 'react'
import ForgotPassword from './page';

export const metadata = {
    title: "Notebook | Forgot Password",
  };

const layout = () => {
  return (
    <ForgotPassword />
  )
}

export default layout